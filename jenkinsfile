pipeline {
    agent any

    stages {
        stage('test') {
            steps {
                echo "-- Running integration tests"
                withNPMwrapper(){
                    npm 'install'
                    npm 'test'
                }

                echo "-- Integration tests complete"
            }
        }
    }
}