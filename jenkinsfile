pipeline {
    agent any

    stages {
        stage('test') {
            steps {
                echo "-- Running integration tests"

                sh 'npm test'

                echo "-- Integration tests complete"
            }
        }
    }
}